"use strict"  //treat all the code as a new version of JS.

// alert(3+3) //throw error as we are using node not browser

console.log(3+3)
console.log("3"+3)

let name = "Dhruv"
let isLoggedIn = true
let age = 20

/*
    integer => 2^52
    bigint
    string => "" || ''
    boolean => true || false
    null => standalone value
    undefined => when val is not assigned till now....
    symbol => uniqueness...

    object

*/

console.log(typeof(3))
console.log(typeof(isLoggedIn))
console.log(typeof(null))